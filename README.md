Program that deletes metadata from PDF and JPEG files.

To install: 
<br>
git clone <repository url>
<br>
python -m venv .venv
<br>
source .venv/bin/activate
<br>
pip install -r requirements.txt
<br>
python3 main.py
